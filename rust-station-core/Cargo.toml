[package]
name = "rust-station-core"
version = "0.1.0"
edition = "2024"

[lib]

[dependencies]
thiserror = "2.0.18"
getrandom = { version = "0.4.2", features = ["wasm_js"] }
rand = { version = "0.10.1", features = ["thread_rng"] }
serde = { version = "1.0.228", features = ["derive"] }
